---
description: Run tests, analyze results, and suggest improvements
argument-hint: [scope (optional)]
disable-model-invocation: true
---

Use the `@tester` subagent to run the project's test suite and analyze the results. If tests fail, investigate the failures and suggest fixes. If coverage data is available, identify areas with low coverage.

$ARGUMENTS
