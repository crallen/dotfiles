---
description: Build, update, or fix frontend UI components and pages
agent: frontend-engineer
subtask: true
---

Implement or modify frontend code for the following request. This includes UI components, pages, forms, layouts, styling, state management, accessibility improvements, and responsive design work. Follow the project's existing frontend patterns and conventions.

Start by identifying the target screen/component, nearby UI precedent, important states, and any missing product or interaction context. If the request is under-specified in a way that materially changes the result, ask concise clarifying questions. If it is really a broader workflow, IA, or design-direction problem, say so plainly and recommend `@architect` or `/spec`.

Use `frontend-patterns` as a router, consult only the relevant `frontend-patterns/reference/*` material, and avoid generic AI-polished UI. Reuse the existing visual system before inventing new patterns. When you finish, report what you verified directly across states, responsiveness, accessibility, and tests/build, plus any remaining uncertainty.

$ARGUMENTS
