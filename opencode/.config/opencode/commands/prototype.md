---
description: Build a throwaway prototype to explore a design question — routes to a terminal app for state/logic questions or multiple UI layout variants for visual questions
agent: tech-lead
subtask: true
---

Load the `prototype-methodology` skill and build a prototype for the question or design below. Determine which branch fits (logic or UI), state the question being answered explicitly before starting, and hand it over to the user to drive once it's runnable. For the logic branch, delegate to `@backend-engineer`; for the UI branch, delegate to `@frontend-engineer`. If the branch is genuinely unclear, ask first.

Current repository state:
!`git status --short`

$ARGUMENTS
